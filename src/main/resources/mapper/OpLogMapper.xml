<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.wuxianjie.springrestapi.shared.operationlog.mapper.OpLogMapper">
  <insert id="insert">
    insert into op_log (request_time, request_ip, end_point, username, message, method, params)
    values (#{requestTime}, #{requestIp}, #{endPoint}, #{username}, #{message}, #{message}, #{params})
  </insert>
</mapper>
